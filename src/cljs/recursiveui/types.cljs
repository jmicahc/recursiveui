(ns recursiveui.types)

(def base-element [:div {}])


(def basic-component [:div {}
                      [:div {:id "toobar"}]
                      [:div {:id "actions"}]])

(def toolbar-path [1])
(def actions [2])


